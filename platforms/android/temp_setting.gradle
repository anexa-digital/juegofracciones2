android {
            defaultConfig {
                applicationId = "org.nativescript.crmmobile"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }